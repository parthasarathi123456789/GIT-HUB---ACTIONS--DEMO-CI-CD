name: Example Variables

on: github_dispatch

env:
  cloud: google-cloud

jobs:
  example:
    runs-on: ubuntu-latest
    env:
      JOB_ENV: "I am job level"
    steps:
      - name: Print variables
        run: |
          echo "Global: $GLOBAL_ENV"
          echo "Job: $JOB_ENV"
          echo "Step: ${{the project_id}}"
        env:
          STEP_ENV: "I am step level"

